# Base URL for the backend API (used if you run client and server on different ports)
# In dev with Vite proxy this can be left empty — requests go to /api automatically.
VITE_API_BASE_URL=http://localhost:5000

# Socket.IO server URL — must match the backend server origin.
VITE_SOCKET_URL=http://localhost:5000
