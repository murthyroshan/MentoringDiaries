# Feature Modules

This folder is reserved for domain-oriented modules (for example `auth`, `diary`, `mentor`, `admin`).
Phase 1 introduces the folder as part of the cleanup baseline; features can be migrated incrementally.
